#include "geneticAlgo.h"

// Petit souvenir de d'avril
/* dummy default constructor that initializes the instance to non trivial content (“Hello world”).

customString::customString()
{
	char s[] = "Hello World";
	int i = 0;
	myString = new char[12];
	while (s[i]!='\0'){
		myString[i]=s[i];
		i++;
	}
	myString[i]='\0';
	cap = 12;
}; */

/* Constructor of the class by default */
